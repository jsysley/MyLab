---
title: "Summary"
author: "jsysley_mac"
date: "2016年9月18日"
output: html_document
---

#Summary

##数据分析
Hmisc()包：describe()
fBasics包的basicStats()

##Linear Regression
不用包，用lm()函数，glm()函数

##关联分析
Arules、arulesViz包
apriori()、eclat()

##聚类分析
Cluster、mclust、stats包

##判别分析
MASS、klaR、class、kknn包

##决策树
rpart、rpart.plot，maptree,RWeka,PWake包

* CART(Classification and Regression Trees)
    
        * rpart：rpart()、prune.rpart()、post()
        
        * rpart.plot: rpart.plot()
        
        * maptree: draw.tree()
        
    * C4.5(successor of ID3)
        
        *RWeka: J48()

##集成学习
adabag包

* bagging(formula,data,mfinal=100)
    *预测：predict(object,data)

*boosting(formula,data,mfinal=5)

##随机森林
randomForest包

* importance(object)
* MDSplot(object,factors)
* rflmpute()
    * rfImpute(x,y,iter=5,ntree=300,...)
    
    * rfImpute(x,data,...,subset)
* treesize(x,terminal=TRUE)
* randomForest(formula,data)
* 可视化：plot()
##支持向量机
e1071包

##神经网络
nnet包

##glmnet包进行LASSO变量选择
glmnet()